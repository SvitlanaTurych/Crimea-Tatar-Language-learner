<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane prefHeight="800.0" prefWidth="1200.0" style="-fx-background-color: #f4f4f4;" xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" fx:controller="qirim.app.controllers.QuizController">

    <!-- 🔸 Центр: Запитання, Варіанти та Результати -->
    <center>
        <VBox alignment="CENTER" spacing="20.0" style="-fx-padding: 40px;">

            <!-- Панель у верхній частині центру для навігації та рахунку -->
            <HBox alignment="CENTER_LEFT" spacing="20.0" maxWidth="700.0">
                <!-- Кнопка "Назад" (завжди видима) -->
                <Button mnemonicParsing="false" onAction="#goBackToHome"
                        style="-fx-background-color: transparent; -fx-text-fill: #333; -fx-font-weight: bold; -fx-cursor: hand;"
                        text="← Назад до тем" />
                <Region HBox.hgrow="ALWAYS" />
                <!-- Мітка для відображення поточного рахунку -->
                <Label fx:id="scoreLabel" text="Бали: 0 / 5" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333;" />
            </HBox>

            <!-- Мітка результату (прихована до кінця тесту) -->
            <Label fx:id="resultMessageLabel"
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #28a745; -fx-alignment: center; -fx-padding: 20px; -fx-background-color: #d4edda; -fx-border-color: #c3e6cb; -fx-border-radius: 8px; -fx-background-radius: 8px;"
                   text="Тест завершено!"
                   visible="false"
                   wrapText="true"
                   maxWidth="700.0" />

            <!-- Панель з питанням -->
            <VBox alignment="CENTER" maxWidth="700.0" style="-fx-border-color: #007bff; -fx-border-width: 2px; -fx-padding: 25px; -fx-background-color: white; -fx-border-radius: 8px; -fx-background-radius: 8px; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 10, 0, 0, 2);">
                <Label fx:id="questionLabel" alignment="CENTER" text="Текст запитання буде тут." wrapText="true" maxWidth="650.0">
                    <font>
                        <Font name="System Bold" size="18.0" />
                    </font>
                </Label>
            </VBox>

            <!-- Контейнер для кнопок відповідей (заповнюється динамічно) -->
            <VBox fx:id="optionsContainer" alignment="CENTER" spacing="12.0" maxWidth="700.0" style="-fx-padding: 10px 0 0 0;" />
        </VBox>
    </center>

    <!-- 🔸 Низ: Навігація та Кнопка Додому (дублювання для кінцевого екрана) -->
    <bottom>
        <VBox alignment="CENTER" spacing="20.0" style="-fx-padding: 20px 0 40px 0;">
            <!-- Кнопка "Назад до дому" (дублювання, visible=false) -->
            <Button fx:id="backToHomeButton" mnemonicParsing="false" onAction="#goBackToHome"
                    style="-fx-font-size: 16px; -fx-background-color: #007bff; -fx-text-fill: white; -fx-padding: 12 35; -fx-cursor: hand; -fx-background-radius: 5px; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.2), 5, 0, 0, 2);"
                    text="Повернутися до тем" visible="false" />

            <!-- Панель навігації між запитаннями -->
            <HBox fx:id="navigationHBox" alignment="CENTER" spacing="20.0">
                <Button fx:id="prevButton" mnemonicParsing="false" onAction="#handlePreviousQuestion" text="← Попереднє"
                        style="-fx-background-color: #6c757d; -fx-text-fill: white; -fx-padding: 10 25; -fx-cursor: hand; -fx-background-radius: 5px; -fx-font-size: 14px;" />
                <Button fx:id="nextButton" mnemonicParsing="false" onAction="#handleNextQuestion" text="Наступне →"
                        style="-fx-background-color: #28a745; -fx-text-fill: white; -fx-padding: 10 25; -fx-cursor: hand; -fx-background-radius: 5px; -fx-font-size: 14px;" />
            </HBox>
        </VBox>
    </bottom>
</BorderPane>